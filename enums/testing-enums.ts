export enum PRIORITY {
  LOW = 'p3',
  MEDIUM = 'p2',
  HIGH = 'p1',
  CRITICAL = 'p0',
}

export enum SEVERITY {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical',
}

export enum RISK_LEVEL {
  R0 = 'R0',
  R1 = 'R1',
  R2 = 'R2',
  R3 = 'R3',
  R4 = 'R4',
}
