export enum SM_PLATFORM {
  FACEBOOK = 'Facebook',
  LINKEDIN = 'LinkedIn',
  INSTAGRAM = 'Instagram',
  TWITTER = 'Twitter/X',
  YOUTUBE = 'YouTube',
  TELEGRAM = 'Telegram',
}

export enum PAYMENTMETHOD {
  VISA,
  MASTERCARD,
  ZELLE,
  CASHAPP,
  DISCOVER,
  AMEX,
  PAYPAL,
  STRIPE,
}

export enum ROLE {
  ADMIN,
  MEMBER,
  CUSTOMER,
  CLIENT,
  MANAGER,
}
