export enum SM_PLATFORM {
  FACEBOOK = 'Facebook',
  LINKEDIN = 'LinkedIn',
  INSTAGRAM = 'Instagram',
  TWITTER = 'Twitter/X',
  YOUTUBE = 'YouTube',
  TELEGRAM = 'Telegram',
}

export enum PAYMENTMETHOD {
  VISA,
  MASTERCARD,
  ZELLE,
  CASHAPP,
  DISCOVER,
  AMEX,
  PAYPAL,
  STRIPE,
}

export enum ROLE {
  ADMIN,
  REGISTERED_USER,
  CUSTOMER,
  CLIENT,
  MANAGER,
  GUEST_USER,
}

export enum ACCT_STATUS {
  ACTIVE,
  DEACTIVATED,
}
